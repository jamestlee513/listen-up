//- create class name for the divs

mixin validationErrors(errors)
    if errors   
        div(class=" *******cannot make any errors change later***********") 
            p The following error(s) occured:
            ul
                each error in errors
                    li= error

mixin field(labelText, fieldName, fieldValue)
    div(class="")
        label(class="form_container-form-input-label" for=fieldName)= labelText
        br
        input(type="text" id=fieldName name=fieldName value=fieldValue class="form_container-form-input")
        br
    
            